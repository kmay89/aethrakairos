{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window controls plus self-update.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
