# DEMO SIGNING KEY — COMMITTED ON PURPOSE. THE SECRET BELOW IS PUBLIC.
#
# This exists so the example score in remixes/ verifies straight out of a
# clone, and so the test suite has something to sign with. Anyone reading
# this repository can forge signatures with it, which means a signature from
# this key proves NOTHING about who made something.
#
# signing.py lists the matching public key in BURNED, so every `remix.py
# verify` shouts whenever it appears in TRUST. Do not remove that warning,
# and do not issue a real grant with this key.
#
# Your own key:  python3 signing.py keygen --out keys/you --name "Your Name"
# (keys/ is gitignored, which is the point.)
ed25519-secret:JSVUVQuc/7oJdvP2bL45Ndppb8KqZ2Q8/yUXzYZvOhg=
